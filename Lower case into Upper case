#include<stdio.h>
int main()
{
    char ch[100];
    scanf("%[^\n]s", ch);
    for(int i =0; ch[i] != '\0'; i++)
    {
        if(ch[i] >= 'a' && ch[i] <= 'z')
            ch[i] = ch[i] - 32;
    }
    printf("%s", ch);
}
