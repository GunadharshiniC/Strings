#include<stdio.h>
int main()
{
    char str[100];
    scanf("%[^\n]s", str);
    scanf("\n");
    char ch;
    scanf("%c", &ch);
    for(int i =0; str[i] != '\0'; i++)
    {
        if(str[i] == ' ')
            str[i] = ch;
    }
    printf("%s", str);
}
