#include<stdio.h>
int main()
{
    char ch[100];
    scanf("%[^\n]s", ch);
    int count = 1;
    for(int i =0; ch[i] != '\0'; i++)
    {
      if(ch[i] == ' ')
          count++;
    }
    printf("%d", count);
}
